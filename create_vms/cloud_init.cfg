users:
  - name: ${user}
    sudo: ALL=(ALL) NOPASSWD:ALL
    groups: users, admin
    home: /home/${user}
    shell: /bin/bash
    ssh-authorized-keys:
      - ${public_key}
ssh_pwauth: false
disable_root: false
chpasswd:
  expire: false




